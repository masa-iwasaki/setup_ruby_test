name: ruby-setup

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v1
    
    - name: Set up Ruby
      uses: masa-iwasaki/setup-ruby@f45b16eac6b4b027a0c091dfdfb66c159a829d94

    - name: Run 'bundle install'
      run: |
        bundle install
    